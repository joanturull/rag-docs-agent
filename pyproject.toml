[project]
name = "rag-docs-agent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Joan Turull", email = "joanturull4@gmail.com" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
rag-docs-agent = "rag_docs_agent:main"

[build-system]
requires = ["uv_build>=0.12.9,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
